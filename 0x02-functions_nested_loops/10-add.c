#include "main.h"

/**
* add - function that check for lowercase and uppercase
* @a: parameter a to be checked
* @b: parameter b to be checked
* Return: a + b
*/

int add(int a, int b)
{

	return (a + b);
}
