#include "main.h"

/**
*  _isdigit - function checking for uppercase
*@c: parameter defined
* Return: 1 if c is digit
*/

int _isdigit(int c)
{

	if (c >= 48 && c <= 57)
		return (1);
	else
		return (0);


}
